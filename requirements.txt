# Runtime dependencies
# Confirmed PEFT version: 0.17.1 (source: docs/evidence-ledger.md §2)
# All other versions: minimum compatible constraints (exact training versions unrecorded).
# See docs/evidence-ledger.md §10 (Reproducibility Risks) for context.

# Deep learning
torch>=2.1.0
accelerate>=0.28.0

# HuggingFace ecosystem
transformers>=4.40.0
datasets>=2.19.0
evaluate>=0.4.1
peft==0.17.1

# Evaluation metrics
scikit-learn>=1.3.0

# Utilities
numpy>=1.24.0,<3.0.0
matplotlib>=3.7.0
tqdm>=4.65.0
